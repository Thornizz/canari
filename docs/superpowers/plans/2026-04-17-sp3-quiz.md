# Canari — Sous-projet 3 : Mode Quiz Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construire le mode quiz complet — génération de questions, écran de question avec timer 15s, écran de résultats avec score, streak, badges, et sauvegarde Supabase.

**Architecture:** `lib/quiz.ts` génère les questions (10 oiseaux aléatoires, 4 choix dont 3 distracteurs préférablement de même famille). `lib/quizSession.ts` est un store module-level qui partage l'état de la session entre les screens de navigation (pas de Context, pas d'URL params complexes). `lib/badges.ts` et `lib/streak.ts` contiennent la logique de gamification. Les trois screens du quiz (`index`, `[questionIndex]`, `resultats`) remplacent les placeholders existants.

**Tech Stack:** Expo SDK 54, Expo Router v4, NativeWind v4, Supabase (tables `quiz_scores`, `badges`, `profiles`), `lib/birds.ts` déjà en place, types `QuizQuestion`, `QuizResult`, `BadgeKey`, `BadgeDefinition` dans `lib/types.ts`

> **Note :** Pas de tests automatisés (focus UI/navigation). La validation se fait visuellement via `npx expo start`.

---

## Fichiers créés / modifiés

| Fichier | Rôle |
|---------|------|
| `lib/quiz.ts` | `generateQuiz()` → 10 `QuizQuestion` aléatoires |
| `lib/quizSession.ts` | Store module-level pour partager l'état entre screens |
| `lib/badges.ts` | `BADGE_DEFINITIONS` + `checkNewBadges()` |
| `lib/streak.ts` | `updateStreak()` — met à jour `profiles` dans Supabase |
| `app/quiz/index.tsx` | Écran de lancement — meilleur score + bouton Commencer |
| `app/quiz/[questionIndex].tsx` | Écran question — photo, 4 choix, timer 15s, feedback |
| `app/quiz/resultats.tsx` | Écran résultats — score, streak, badge, récap, boutons |

---

### Task 1 : Logique de génération de quiz (`lib/quiz.ts` + `lib/quizSession.ts`)

**Files:**
- Create: `lib/quiz.ts`
- Create: `lib/quizSession.ts`

- [ ] **Step 1 : Créer `lib/quiz.ts`**

```ts
import { allBirds } from "@/lib/birds";
import type { QuizQuestion } from "@/lib/types";

/** Mélange un tableau en place (Fisher-Yates) et le retourne */
function shuffle<T>(arr: T[]): T[] {
  const a = [...arr];
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}

/**
 * Génère 10 questions de quiz.
 * Pour chaque oiseau tiré : 3 distracteurs choisis en priorité dans la même famille,
 * complétés par des oiseaux d'autres familles si nécessaire.
 */
export function generateQuiz(): QuizQuestion[] {
  const selected = shuffle(allBirds).slice(0, 10);

  return selected.map((bird): QuizQuestion => {
    const sameFamily = allBirds.filter(
      (b) => b.famille === bird.famille && b.id !== bird.id
    );
    const otherBirds = allBirds.filter((b) => b.famille !== bird.famille);
    const distractorPool = shuffle([...sameFamily, ...otherBirds]);
    const distractors = distractorPool.slice(0, 3);

    // Insère l'oiseau correct à une position aléatoire dans les 4 choix
    const correctIndex = Math.floor(Math.random() * 4);
    const choices = [...distractors];
    choices.splice(correctIndex, 0, bird);

    return { bird, choices, correctIndex };
  });
}
```

- [ ] **Step 2 : Créer `lib/quizSession.ts`**

```ts
import type { QuizQuestion, QuizResult } from "@/lib/types";

let _questions: QuizQuestion[] = [];
let _results: QuizResult[] = [];

export const quizSession = {
  /** Démarre une nouvelle session avec les questions générées */
  start(questions: QuizQuestion[]): void {
    _questions = questions;
    _results = [];
  },

  /** Retourne la question à l'index donné, ou undefined si hors limites */
  getQuestion(index: number): QuizQuestion | undefined {
    return _questions[index];
  },

  /** Enregistre le résultat d'une question */
  recordResult(result: QuizResult): void {
    _results.push(result);
  },

  /** Retourne une copie des résultats enregistrés */
  getResults(): QuizResult[] {
    return [..._results];
  },

  totalQuestions(): number {
    return _questions.length;
  },
};
```

- [ ] **Step 3 : Commit**

```bash
cd /Users/I531480/SAPDevelop/git/canari
git add lib/quiz.ts lib/quizSession.ts
git commit -m "feat: add quiz generation and session store"
```

---

### Task 2 : Logique de badges (`lib/badges.ts`)

**Files:**
- Create: `lib/badges.ts`

- [ ] **Step 1 : Créer `lib/badges.ts`**

```ts
import { allBirds } from "@/lib/birds";
import type { BadgeKey, BadgeDefinition } from "@/lib/types";

export const BADGE_DEFINITIONS: BadgeDefinition[] = [
  { key: "premier_quiz",    label: "Premier envol",          description: "1er quiz complété",                   emoji: "🐣" },
  { key: "score_parfait",   label: "Ornithologue parfait",   description: "Score 10/10",                         emoji: "🎯" },
  { key: "streak_3",        label: "3 jours de suite",       description: "Streak ≥ 3 jours",                   emoji: "🔥" },
  { key: "streak_7",        label: "Une semaine !",          description: "Streak ≥ 7 jours",                   emoji: "🔥🔥" },
  { key: "streak_30",       label: "Passionné",              description: "Streak ≥ 30 jours",                  emoji: "🏆" },
  { key: "score_7",         label: "Apprenti ornithologue",  description: "Score ≥ 7 pour la 1ère fois",        emoji: "🏅" },
  { key: "oiseaux_50",      label: "Explorateur",            description: "50 oiseaux vus",                     emoji: "🌿" },
  { key: "oiseaux_100",     label: "Grand explorateur",      description: "100 oiseaux vus",                    emoji: "🌍" },
  { key: "famille_complete",label: "Expert famille",         description: "Tous les oiseaux d'une famille vus", emoji: "⭐" },
];

export interface BadgeCheckParams {
  score: number;          // Score du quiz (0–10)
  streak: number;         // Streak après mise à jour
  seenBirdIds: string[];  // Tous les IDs d'oiseaux vus (toutes sessions confondues)
  earnedKeys: BadgeKey[]; // Badges déjà obtenus (depuis la DB)
  isFirstQuiz: boolean;   // true si c'est le tout premier quiz de l'utilisateur
}

/** Retourne les clés des badges nouvellement débloqués (pas encore dans earnedKeys) */
export function checkNewBadges(params: BadgeCheckParams): BadgeKey[] {
  const { score, streak, seenBirdIds, earnedKeys, isFirstQuiz } = params;
  const earned = new Set(earnedKeys);
  const newBadges: BadgeKey[] = [];

  const earn = (key: BadgeKey) => {
    if (!earned.has(key)) {
      newBadges.push(key);
      earned.add(key);
    }
  };

  if (isFirstQuiz)      earn("premier_quiz");
  if (score === 10)     earn("score_parfait");
  if (score >= 7)       earn("score_7");
  if (streak >= 3)      earn("streak_3");
  if (streak >= 7)      earn("streak_7");
  if (streak >= 30)     earn("streak_30");
  if (seenBirdIds.length >= 50)  earn("oiseaux_50");
  if (seenBirdIds.length >= 100) earn("oiseaux_100");

  // famille_complete : au moins une famille dont tous les oiseaux sont vus
  const seenSet = new Set(seenBirdIds);
  const families = [...new Set(allBirds.map((b) => b.famille))];
  const hasCompleteFamily = families.some((famille) =>
    allBirds.filter((b) => b.famille === famille).every((b) => seenSet.has(b.id))
  );
  if (hasCompleteFamily) earn("famille_complete");

  return newBadges;
}
```

- [ ] **Step 2 : Commit**

```bash
cd /Users/I531480/SAPDevelop/git/canari
git add lib/badges.ts
git commit -m "feat: add badge definitions and check logic"
```

---

### Task 3 : Logique de streak (`lib/streak.ts`)

**Files:**
- Create: `lib/streak.ts`

- [ ] **Step 1 : Créer `lib/streak.ts`**

```ts
import type { SupabaseClient } from "@supabase/supabase-js";

export interface StreakResult {
  newStreak: number;
  previousStreak: number;
  wasUpdatedToday: boolean; // true si déjà joué aujourd'hui (pas de changement)
}

/**
 * Met à jour le streak de l'utilisateur dans Supabase.
 * Règles :
 *   - Déjà joué aujourd'hui → pas de changement
 *   - Joué hier → streak + 1
 *   - Sinon (jour sauté) → streak remis à 1
 */
export async function updateStreak(
  userId: string,
  supabase: SupabaseClient
): Promise<StreakResult> {
  const { data: profile } = await supabase
    .from("profiles")
    .select("streak_count, last_played_date")
    .eq("id", userId)
    .single();

  if (!profile) return { newStreak: 1, previousStreak: 0, wasUpdatedToday: false };

  const today = new Date().toISOString().split("T")[0]; // "YYYY-MM-DD"
  const previousStreak = profile.streak_count ?? 0;

  if (profile.last_played_date === today) {
    return { newStreak: previousStreak, previousStreak, wasUpdatedToday: true };
  }

  const yesterday = new Date();
  yesterday.setDate(yesterday.getDate() - 1);
  const yesterdayStr = yesterday.toISOString().split("T")[0];

  const newStreak =
    profile.last_played_date === yesterdayStr ? previousStreak + 1 : 1;

  await supabase
    .from("profiles")
    .update({ streak_count: newStreak, last_played_date: today })
    .eq("id", userId);

  return { newStreak, previousStreak, wasUpdatedToday: false };
}
```

- [ ] **Step 2 : Commit**

```bash
cd /Users/I531480/SAPDevelop/git/canari
git add lib/streak.ts
git commit -m "feat: add streak update logic"
```

---

### Task 4 : Écran de lancement du quiz (`app/quiz/index.tsx`)

**Files:**
- Modify: `app/quiz/index.tsx` (remplacer le placeholder)

Cet écran affiche le meilleur score personnel (si connecté) et un bouton "Commencer" qui génère les questions et navigue vers `/quiz/0`.

- [ ] **Step 1 : Écrire `app/quiz/index.tsx`**

```tsx
import { View, Text, TouchableOpacity } from "react-native";
import { router } from "expo-router";
import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";
import { generateQuiz } from "@/lib/quiz";
import { quizSession } from "@/lib/quizSession";

export default function QuizIndexScreen() {
  const [bestScore, setBestScore] = useState<number | null>(null);
  const [userId, setUserId] = useState<string | null>(null);

  useEffect(() => {
    supabase.auth.getSession().then(({ data: { session } }) => {
      if (session) {
        setUserId(session.user.id);
        loadBestScore(session.user.id);
      }
    });
    const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, session) => {
      if (session) {
        setUserId(session.user.id);
        loadBestScore(session.user.id);
      } else {
        setUserId(null);
        setBestScore(null);
      }
    });
    return () => subscription.unsubscribe();
  }, []);

  const loadBestScore = async (uid: string) => {
    const { data } = await supabase
      .from("quiz_scores")
      .select("score")
      .eq("user_id", uid)
      .order("score", { ascending: false })
      .limit(1)
      .maybeSingle();
    if (data) setBestScore(data.score);
  };

  const handleStart = () => {
    const questions = generateQuiz();
    quizSession.start(questions);
    router.push("/quiz/0");
  };

  return (
    <View className="flex-1 bg-stone-900">
      {/* Header */}
      <View className="pt-14 pb-3 px-4 flex-row items-center gap-3 border-b border-stone-800">
        <TouchableOpacity onPress={() => router.back()} className="p-2 -ml-2">
          <Text className="text-amber-500 text-lg">‹</Text>
        </TouchableOpacity>
        <Text className="text-white text-xl font-bold flex-1">Mode Quiz</Text>
      </View>

      {/* Contenu centré */}
      <View className="flex-1 items-center justify-center px-6 gap-6">
        {/* Icône */}
        <Text className="text-7xl">🎯</Text>

        {/* Description */}
        <View className="items-center gap-2">
          <Text className="text-white text-2xl font-bold text-center">
            Teste tes connaissances
          </Text>
          <Text className="text-stone-400 text-base text-center">
            10 questions · 15 secondes par question
          </Text>
          <Text className="text-stone-500 text-sm text-center">
            4 propositions, 1 seule bonne réponse
          </Text>
        </View>

        {/* Meilleur score */}
        {bestScore !== null && (
          <View className="bg-amber-500/10 border border-amber-500/25 rounded-2xl px-8 py-4 items-center">
            <Text className="text-amber-400 text-xs uppercase tracking-widest mb-1">
              Ton meilleur score
            </Text>
            <Text className="text-white text-4xl font-black">
              {bestScore}
              <Text className="text-stone-400 text-xl font-normal">/10</Text>
            </Text>
          </View>
        )}

        {/* Bouton Commencer */}
        <TouchableOpacity
          className="bg-amber-500 rounded-2xl px-12 py-4 w-full items-center"
          onPress={handleStart}
          activeOpacity={0.85}
        >
          <Text className="text-stone-900 text-xl font-black">Commencer</Text>
        </TouchableOpacity>

        {!userId && (
          <Text className="text-stone-600 text-xs text-center">
            Connecte-toi pour sauvegarder ton score
          </Text>
        )}
      </View>
    </View>
  );
}
```

- [ ] **Step 2 : Commit**

```bash
cd /Users/I531480/SAPDevelop/git/canari
git add app/quiz/index.tsx
git commit -m "feat: build quiz launch screen with best score display"
```

---

### Task 5 : Écran de question (`app/quiz/[questionIndex].tsx`)

**Files:**
- Create: `app/quiz/[questionIndex].tsx`

Cet écran affiche une question du quiz : grande photo, 4 choix en grille 2×2, timer 15s (barre qui se vide), feedback coloré immédiat au tap, puis auto-avance après 1s.

- [ ] **Step 1 : Créer `app/quiz/[questionIndex].tsx`**

```tsx
import { View, Text, TouchableOpacity, Image } from "react-native";
import { useLocalSearchParams, router } from "expo-router";
import { useEffect, useState } from "react";
import { quizSession } from "@/lib/quizSession";
import type { QuizQuestion } from "@/lib/types";

const TOTAL = 10;
const TIMER_SECONDS = 15;

export default function QuizQuestionScreen() {
  const { questionIndex: rawIndex } = useLocalSearchParams<{ questionIndex: string }>();
  const questionIndex = parseInt(rawIndex ?? "0", 10);
  const question: QuizQuestion | undefined = quizSession.getQuestion(questionIndex);

  const [timeLeft, setTimeLeft] = useState(TIMER_SECONDS);
  const [selectedIndex, setSelectedIndex] = useState<number | null>(null);
  const [answered, setAnswered] = useState(false);

  // Timer countdown
  useEffect(() => {
    if (answered) return;
    if (timeLeft <= 0) {
      handleAnswer(null); // timeout → faux
      return;
    }
    const t = setTimeout(() => setTimeLeft((prev) => prev - 1), 1000);
    return () => clearTimeout(t);
  }, [timeLeft, answered]);

  // Auto-advance après réponse
  useEffect(() => {
    if (!answered) return;
    const t = setTimeout(() => {
      if (questionIndex < TOTAL - 1) {
        router.replace(`/quiz/${questionIndex + 1}` as any);
      } else {
        router.replace("/quiz/resultats");
      }
    }, 1000);
    return () => clearTimeout(t);
  }, [answered]);

  const handleAnswer = (idx: number | null) => {
    if (answered) return;
    setAnswered(true);
    setSelectedIndex(idx);
    if (!question) return;
    quizSession.recordResult({
      question,
      selectedIndex: idx,
      correct: idx === question.correctIndex,
    });
  };

  if (!question) {
    return (
      <View className="flex-1 items-center justify-center bg-stone-900">
        <Text className="text-stone-400">Question introuvable</Text>
      </View>
    );
  }

  const timerPercent = (timeLeft / TIMER_SECONDS) * 100;

  return (
    <View className="flex-1 bg-stone-900">
      {/* Barre de progression + timer */}
      <View className="pt-14 px-4 pb-3 gap-2">
        <View className="flex-row items-center justify-between">
          <Text className="text-stone-400 text-sm">
            Question{" "}
            <Text className="text-white font-bold">{questionIndex + 1}</Text>
            <Text className="text-stone-500">/{TOTAL}</Text>
          </Text>
          <Text className={`text-sm font-bold ${timeLeft <= 5 ? "text-red-400" : "text-amber-400"}`}>
            ⏱ {timeLeft}s
          </Text>
        </View>
        {/* Barre de progression questions */}
        <View className="h-1.5 bg-stone-800 rounded-full overflow-hidden">
          <View
            className="h-full bg-amber-500 rounded-full"
            style={{ width: `${((questionIndex + 1) / TOTAL) * 100}%` }}
          />
        </View>
        {/* Barre timer */}
        <View className="h-1 bg-stone-800 rounded-full overflow-hidden">
          <View
            className={`h-full rounded-full ${timeLeft <= 5 ? "bg-red-500" : "bg-emerald-500"}`}
            style={{ width: `${timerPercent}%` }}
          />
        </View>
      </View>

      {/* Grande photo */}
      <Image
        source={{ uri: question.bird.imageUrl }}
        className="w-full"
        style={{ height: 240 }}
        resizeMode="cover"
      />

      {/* Question */}
      <View className="px-4 py-3">
        <Text className="text-white text-lg font-semibold text-center">
          Quel est cet oiseau ?
        </Text>
      </View>

      {/* 4 choix en grille 2×2 */}
      <View className="px-4 flex-1 gap-3">
        <View className="flex-row gap-3">
          <ChoiceButton
            bird={question.choices[0]}
            index={0}
            correctIndex={question.correctIndex}
            selectedIndex={selectedIndex}
            answered={answered}
            onPress={() => handleAnswer(0)}
          />
          <ChoiceButton
            bird={question.choices[1]}
            index={1}
            correctIndex={question.correctIndex}
            selectedIndex={selectedIndex}
            answered={answered}
            onPress={() => handleAnswer(1)}
          />
        </View>
        <View className="flex-row gap-3">
          <ChoiceButton
            bird={question.choices[2]}
            index={2}
            correctIndex={question.correctIndex}
            selectedIndex={selectedIndex}
            answered={answered}
            onPress={() => handleAnswer(2)}
          />
          <ChoiceButton
            bird={question.choices[3]}
            index={3}
            correctIndex={question.correctIndex}
            selectedIndex={selectedIndex}
            answered={answered}
            onPress={() => handleAnswer(3)}
          />
        </View>
      </View>

      <View className="h-8" />
    </View>
  );
}

interface ChoiceButtonProps {
  bird: { nomFr: string };
  index: number;
  correctIndex: number;
  selectedIndex: number | null;
  answered: boolean;
  onPress: () => void;
}

function ChoiceButton({ bird, index, correctIndex, selectedIndex, answered, onPress }: ChoiceButtonProps) {
  let bgColor = "bg-stone-800";
  let borderColor = "border-stone-700";
  let textColor = "text-white";
  let prefix = "";

  if (answered) {
    if (index === correctIndex) {
      bgColor = "bg-emerald-900";
      borderColor = "border-emerald-500";
      textColor = "text-emerald-300";
      prefix = "✓ ";
    } else if (index === selectedIndex) {
      bgColor = "bg-red-900";
      borderColor = "border-red-500";
      textColor = "text-red-300";
      prefix = "✗ ";
    }
  }

  return (
    <TouchableOpacity
      className={`flex-1 ${bgColor} border ${borderColor} rounded-xl p-3 items-center justify-center min-h-[60px]`}
      onPress={onPress}
      disabled={answered}
      activeOpacity={0.75}
    >
      <Text className={`${textColor} text-sm font-semibold text-center`}>
        {prefix}{bird.nomFr}
      </Text>
    </TouchableOpacity>
  );
}
```

- [ ] **Step 2 : Commit**

```bash
cd /Users/I531480/SAPDevelop/git/canari
git add "app/quiz/[questionIndex].tsx"
git commit -m "feat: build quiz question screen with timer and feedback"
```

---

### Task 6 : Écran de résultats (`app/quiz/resultats.tsx`)

**Files:**
- Modify: `app/quiz/resultats.tsx` (remplacer le placeholder)

Cet écran récupère les résultats depuis `quizSession`, calcule le score, sauvegarde en Supabase, met à jour le streak, vérifie les nouveaux badges, puis affiche le tout.

- [ ] **Step 1 : Écrire `app/quiz/resultats.tsx`**

```tsx
import { View, Text, TouchableOpacity, ScrollView } from "react-native";
import { router } from "expo-router";
import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";
import { quizSession } from "@/lib/quizSession";
import { updateStreak } from "@/lib/streak";
import { checkNewBadges, BADGE_DEFINITIONS } from "@/lib/badges";
import type { QuizResult, BadgeKey, BadgeDefinition } from "@/lib/types";

export default function QuizResultatsScreen() {
  const results: QuizResult[] = quizSession.getResults();
  const score = results.filter((r) => r.correct).length;

  const [streak, setStreak] = useState(0);
  const [newBadges, setNewBadges] = useState<BadgeDefinition[]>([]);
  const [saved, setSaved] = useState(false);

  useEffect(() => {
    supabase.auth.getSession().then(({ data: { session } }) => {
      if (session) saveResults(session.user.id);
      else setSaved(true);
    });
  }, []);

  const saveResults = async (userId: string) => {
    // 1. Sauvegarder le score
    await supabase.from("quiz_scores").insert({ user_id: userId, score });

    // 2. Mettre à jour le streak
    const { newStreak } = await updateStreak(userId, supabase);
    setStreak(newStreak);

    // 3. Récupérer les données nécessaires pour les badges
    const [badgesRes, progressRes] = await Promise.all([
      supabase.from("badges").select("badge_key").eq("user_id", userId),
      supabase.from("bird_progress").select("bird_id").eq("user_id", userId),
    ]);

    const earnedKeys = (badgesRes.data ?? []).map((b) => b.badge_key as BadgeKey);
    const seenBirdIds = (progressRes.data ?? []).map((p) => p.bird_id as string);
    const isFirstQuiz = earnedKeys.length === 0;

    // 4. Calculer les nouveaux badges
    const newKeys = checkNewBadges({
      score,
      streak: newStreak,
      seenBirdIds,
      earnedKeys,
      isFirstQuiz,
    });

    // 5. Insérer les nouveaux badges
    if (newKeys.length > 0) {
      await supabase.from("badges").insert(
        newKeys.map((badge_key) => ({ user_id: userId, badge_key }))
      );
      const defs = newKeys
        .map((key) => BADGE_DEFINITIONS.find((d) => d.key === key))
        .filter((d): d is BadgeDefinition => d !== undefined);
      setNewBadges(defs);
    }

    setSaved(true);
  };

  const scoreColor = score >= 8 ? "#f59e0b" : score >= 5 ? "#3b82f6" : "#ef4444";

  return (
    <ScrollView className="flex-1 bg-stone-900" bounces={false}>
      {/* Header score */}
      <View
        className="pt-16 pb-8 px-6 items-center"
        style={{ backgroundColor: scoreColor + "22" }}
      >
        <Text className="text-stone-400 text-xs uppercase tracking-widest mb-2">
          Score final
        </Text>
        <View className="flex-row items-end gap-1 mb-3">
          <Text className="text-white font-black" style={{ fontSize: 80, lineHeight: 88, color: scoreColor }}>
            {score}
          </Text>
          <Text className="text-stone-400 text-2xl mb-4">/10</Text>
        </View>
        {streak > 0 && (
          <View className="bg-amber-500/20 border border-amber-500/40 rounded-full px-4 py-1.5">
            <Text className="text-amber-400 text-sm font-semibold">
              🔥 Série de {streak} jour{streak > 1 ? "s" : ""} !
            </Text>
          </View>
        )}
      </View>

      <View className="px-4 pt-4 pb-10 gap-4">
        {/* Badge débloqué */}
        {newBadges.length > 0 && (
          <View className="bg-stone-800 border border-amber-500/30 rounded-2xl p-4 gap-3">
            <Text className="text-amber-400 text-xs uppercase tracking-widest font-semibold">
              Badge{newBadges.length > 1 ? "s" : ""} débloqué{newBadges.length > 1 ? "s" : ""} !
            </Text>
            {newBadges.map((badge) => (
              <View key={badge.key} className="flex-row items-center gap-3">
                <Text className="text-4xl">{badge.emoji}</Text>
                <View className="flex-1">
                  <Text className="text-white font-bold text-sm">{badge.label}</Text>
                  <Text className="text-stone-400 text-xs">{badge.description}</Text>
                </View>
              </View>
            ))}
          </View>
        )}

        {/* Récap des 10 questions */}
        <View className="gap-2">
          <Text className="text-stone-400 text-xs uppercase tracking-widest font-semibold mb-1">
            Récapitulatif
          </Text>
          {results.map((result, i) => (
            <View
              key={i}
              className="flex-row items-center justify-between bg-stone-800 rounded-xl px-4 py-3"
            >
              <View className="flex-1">
                <Text className="text-white text-sm font-medium">
                  {result.question.bird.nomFr}
                </Text>
                {!result.correct && result.selectedIndex !== null && (
                  <Text className="text-stone-500 text-xs">
                    Répondu : {result.question.choices[result.selectedIndex].nomFr}
                  </Text>
                )}
                {!result.correct && result.selectedIndex === null && (
                  <Text className="text-stone-500 text-xs">Temps écoulé</Text>
                )}
              </View>
              <Text className={`text-lg font-bold ${result.correct ? "text-emerald-400" : "text-red-400"}`}>
                {result.correct ? "✓" : "✗"}
              </Text>
            </View>
          ))}
        </View>

        {/* Boutons */}
        <View className="flex-row gap-3 mt-2">
          <TouchableOpacity
            className="flex-1 bg-amber-500 rounded-2xl py-4 items-center"
            onPress={() => {
              const questions = (await import("@/lib/quiz")).generateQuiz();
              quizSession.start(questions);
              router.replace("/quiz/0");
            }}
            activeOpacity={0.85}
          >
            <Text className="text-stone-900 font-black text-base">🎯 Rejouer</Text>
          </TouchableOpacity>
          <TouchableOpacity
            className="flex-1 bg-stone-800 border border-stone-700 rounded-2xl py-4 items-center"
            onPress={() => router.push("/classement")}
            activeOpacity={0.85}
          >
            <Text className="text-white font-semibold text-base">🏆 Classement</Text>
          </TouchableOpacity>
        </View>
      </View>
    </ScrollView>
  );
}
```

> **Note :** Le bouton "Rejouer" utilise un import dynamique pour éviter une dépendance circulaire entre `resultats.tsx` et `quiz.ts`. L'import `generateQuiz` est au niveau du composant pour rester simple.

- [ ] **Step 2 : Corriger le bouton Rejouer (l'import dynamique ne fonctionne pas dans un handler)**

Remplacer la fonction `onPress` du bouton Rejouer par une version synchrone avec import statique en haut du fichier. Modifier l'import au début de `app/quiz/resultats.tsx` pour ajouter `generateQuiz` :

```tsx
import { generateQuiz } from "@/lib/quiz";
```

Et remplacer le `onPress` du bouton Rejouer par :

```tsx
onPress={() => {
  const questions = generateQuiz();
  quizSession.start(questions);
  router.replace("/quiz/0");
}}
```

Le fichier final de `app/quiz/resultats.tsx` (avec correction intégrée) :

```tsx
import { View, Text, TouchableOpacity, ScrollView } from "react-native";
import { router } from "expo-router";
import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";
import { quizSession } from "@/lib/quizSession";
import { generateQuiz } from "@/lib/quiz";
import { updateStreak } from "@/lib/streak";
import { checkNewBadges, BADGE_DEFINITIONS } from "@/lib/badges";
import type { QuizResult, BadgeKey, BadgeDefinition } from "@/lib/types";

export default function QuizResultatsScreen() {
  const results: QuizResult[] = quizSession.getResults();
  const score = results.filter((r) => r.correct).length;

  const [streak, setStreak] = useState(0);
  const [newBadges, setNewBadges] = useState<BadgeDefinition[]>([]);
  const [saved, setSaved] = useState(false);

  useEffect(() => {
    supabase.auth.getSession().then(({ data: { session } }) => {
      if (session) saveResults(session.user.id);
      else setSaved(true);
    });
  }, []);

  const saveResults = async (userId: string) => {
    await supabase.from("quiz_scores").insert({ user_id: userId, score });

    const { newStreak } = await updateStreak(userId, supabase);
    setStreak(newStreak);

    const [badgesRes, progressRes] = await Promise.all([
      supabase.from("badges").select("badge_key").eq("user_id", userId),
      supabase.from("bird_progress").select("bird_id").eq("user_id", userId),
    ]);

    const earnedKeys = (badgesRes.data ?? []).map((b) => b.badge_key as BadgeKey);
    const seenBirdIds = (progressRes.data ?? []).map((p) => p.bird_id as string);
    const isFirstQuiz = earnedKeys.length === 0;

    const newKeys = checkNewBadges({ score, streak: newStreak, seenBirdIds, earnedKeys, isFirstQuiz });

    if (newKeys.length > 0) {
      await supabase.from("badges").insert(
        newKeys.map((badge_key) => ({ user_id: userId, badge_key }))
      );
      const defs = newKeys
        .map((key) => BADGE_DEFINITIONS.find((d) => d.key === key))
        .filter((d): d is BadgeDefinition => d !== undefined);
      setNewBadges(defs);
    }

    setSaved(true);
  };

  const scoreColor = score >= 8 ? "#f59e0b" : score >= 5 ? "#3b82f6" : "#ef4444";

  return (
    <ScrollView className="flex-1 bg-stone-900" bounces={false}>
      {/* Header score */}
      <View
        className="pt-16 pb-8 px-6 items-center"
        style={{ backgroundColor: scoreColor + "22" }}
      >
        <Text className="text-stone-400 text-xs uppercase tracking-widest mb-2">
          Score final
        </Text>
        <View className="flex-row items-end gap-1 mb-3">
          <Text className="text-white font-black" style={{ fontSize: 80, lineHeight: 88, color: scoreColor }}>
            {score}
          </Text>
          <Text className="text-stone-400 text-2xl mb-4">/10</Text>
        </View>
        {streak > 0 && (
          <View className="bg-amber-500/20 border border-amber-500/40 rounded-full px-4 py-1.5">
            <Text className="text-amber-400 text-sm font-semibold">
              🔥 Série de {streak} jour{streak > 1 ? "s" : ""} !
            </Text>
          </View>
        )}
      </View>

      <View className="px-4 pt-4 pb-10 gap-4">
        {/* Badges débloqués */}
        {newBadges.length > 0 && (
          <View className="bg-stone-800 border border-amber-500/30 rounded-2xl p-4 gap-3">
            <Text className="text-amber-400 text-xs uppercase tracking-widest font-semibold">
              Badge{newBadges.length > 1 ? "s" : ""} débloqué{newBadges.length > 1 ? "s" : ""} !
            </Text>
            {newBadges.map((badge) => (
              <View key={badge.key} className="flex-row items-center gap-3">
                <Text className="text-4xl">{badge.emoji}</Text>
                <View className="flex-1">
                  <Text className="text-white font-bold text-sm">{badge.label}</Text>
                  <Text className="text-stone-400 text-xs">{badge.description}</Text>
                </View>
              </View>
            ))}
          </View>
        )}

        {/* Récap des 10 questions */}
        <View className="gap-2">
          <Text className="text-stone-400 text-xs uppercase tracking-widest font-semibold mb-1">
            Récapitulatif
          </Text>
          {results.map((result, i) => (
            <View
              key={i}
              className="flex-row items-center justify-between bg-stone-800 rounded-xl px-4 py-3"
            >
              <View className="flex-1">
                <Text className="text-white text-sm font-medium">
                  {result.question.bird.nomFr}
                </Text>
                {!result.correct && result.selectedIndex !== null && (
                  <Text className="text-stone-500 text-xs">
                    Répondu : {result.question.choices[result.selectedIndex].nomFr}
                  </Text>
                )}
                {!result.correct && result.selectedIndex === null && (
                  <Text className="text-stone-500 text-xs">Temps écoulé</Text>
                )}
              </View>
              <Text className={`text-lg font-bold ${result.correct ? "text-emerald-400" : "text-red-400"}`}>
                {result.correct ? "✓" : "✗"}
              </Text>
            </View>
          ))}
        </View>

        {/* Boutons */}
        <View className="flex-row gap-3 mt-2">
          <TouchableOpacity
            className="flex-1 bg-amber-500 rounded-2xl py-4 items-center"
            onPress={() => {
              const questions = generateQuiz();
              quizSession.start(questions);
              router.replace("/quiz/0");
            }}
            activeOpacity={0.85}
          >
            <Text className="text-stone-900 font-black text-base">🎯 Rejouer</Text>
          </TouchableOpacity>
          <TouchableOpacity
            className="flex-1 bg-stone-800 border border-stone-700 rounded-2xl py-4 items-center"
            onPress={() => router.push("/classement")}
            activeOpacity={0.85}
          >
            <Text className="text-white font-semibold text-base">🏆 Classement</Text>
          </TouchableOpacity>
        </View>
      </View>
    </ScrollView>
  );
}
```

- [ ] **Step 3 : Commit**

```bash
cd /Users/I531480/SAPDevelop/git/canari
git add app/quiz/resultats.tsx
git commit -m "feat: build quiz results screen with score, streak, badges and recap"
```

---

### Self-Review

**Spec coverage :**
- ✅ "10 questions · 15 secondes par question" → TIMER_SECONDS=15, TOTAL=10
- ✅ Grande photo de l'oiseau plein écran → Image height:240
- ✅ "Quel est cet oiseau ?" → texte fixe
- ✅ 4 propositions en grille 2×2 → deux View `flex-row` avec 2 ChoiceButton chacun
- ✅ 1 correcte + 3 distracteurs de la même famille (+ fallback autres familles) → `generateQuiz()` Task 1
- ✅ Feedback coloré immédiat : vert (correct) / rouge (incorrect) → `ChoiceButton` Task 5
- ✅ Après 1s → question suivante automatiquement → `useEffect([answered])` Task 5
- ✅ Si timer expire → compte comme faux, passage automatique → `handleAnswer(null)` Task 5
- ✅ Meilleur score personnel affiché (si connecté) → `loadBestScore` Task 4
- ✅ Bouton "Commencer" jaune → Task 4
- ✅ Header coloré dégradé avec score large → `scoreColor + "22"` Task 6
- ✅ Streak mis à jour → `updateStreak()` Task 6
- ✅ Badge débloqué si applicable → `checkNewBadges()` + affichage Task 6
- ✅ Liste scrollable des 10 questions avec ✓/✗ et réponse correcte si erreur → récap Task 6
- ✅ Boutons Rejouer et Classement → Task 6

**Placeholder scan :** Aucun TBD, TODO, ou description vague. ✅

**Type consistency :**
- `QuizQuestion`, `QuizResult`, `BadgeKey`, `BadgeDefinition` tous importés depuis `lib/types.ts` (existant) ✅
- `quizSession.getResults()` retourne `QuizResult[]`, consommé dans `resultats.tsx` ✅
- `quizSession.getQuestion(index)` retourne `QuizQuestion | undefined`, gardé dans `[questionIndex].tsx` ✅
- `generateQuiz()` retourne `QuizQuestion[]`, passé à `quizSession.start()` ✅
- `updateStreak()` prend `(userId: string, supabase: SupabaseClient)`, appelé avec les bons types ✅
- `checkNewBadges(BadgeCheckParams)` retourne `BadgeKey[]`, consommé dans `resultats.tsx` ✅
